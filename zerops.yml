zerops:
  - setup: nodesyslog
    build:
      os: ubuntu
      base: nodejs@20
      # prepareCommands:
      #  - echo "The application build is being prepared ...."
      buildCommands:
        - npm i
      deployFiles:
        - ./app/index.js
        - node_modules
        - package.json
    run:
      os: ubuntu
      base: nodejs@20
      ports:
        - port: 3000
          httpSupport: true
        - port: 3001
          httpSupport: true
      # prepareCommands:
      #  - echo "The application runtime is being prepared ...."
      initCommands:
        - echo "1 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "2 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "3 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "4 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "5 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "6 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "7 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "8 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "8 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "9 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "10 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "11 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "12 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "13 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "14 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "15 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "16 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "17 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "18 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "19 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "20 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "21 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "22 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "23 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "24 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "25 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "26 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "27 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "28 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "29 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "30 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "31 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "32 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "33 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "34 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "35 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "36 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "37 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "38 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "39 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
        - echo "40 - The application runtime is being initialized .... We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font. We need a long log line to see if multi-line wrapping is correctly shown using mono font."
      # initCommands:
      #  - echo "Follow explicit exit ..."
      #  - sleep 60
      #  - exit 1
      start: npm start

  - setup: nodesyslogha
    extends: nodesyslog